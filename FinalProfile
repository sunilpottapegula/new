<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Gift App Pro</title>
    <style>
        :root {
            --primary: #4f46e5;
            --bg-body: #0f172a; 
            --bg-canvas: #f3f4f6;
            --bg-card: #ffffff;
            --text-dark: #111827;
            --text-light: #6b7280;
            --border: #d1d5db;
            --danger: #ef4444;
            --success: #10b981;
            --modal-bg: rgba(0, 0, 0, 0.5);
            /* UID Specific Light */
            --uid-gradient: linear-gradient(145deg, #ffffff, #f8fafc);
        }

        [data-theme="dark"] {
            --bg-canvas: #111827;
            --bg-card: #1f2937;
            --text-dark: #f9fafb;
            --text-light: #9ca3af;
            --border: #374151;
            /* UID Specific Dark */
            --uid-gradient: linear-gradient(145deg, #1f2937, #111827);
        }

        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Inter', system-ui, sans-serif; }

        body {
            background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            transition: background 0.5s ease;
        }

        .view-controls {
            margin-bottom: 20px;
            background: rgba(255, 255, 255, 0.1);
            padding: 10px 20px;
            border-radius: 50px;
            display: flex;
            align-items: center; gap: 15px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            z-index: 100;
        }

        .app-container {
            width: 100%;
            max-width: 800px;
            height: 700px;
            background: var(--bg-canvas);
            border-radius: 24px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
        }

        .app-container.mobile-mode {
            max-width: 375px;
            height: 667px;
            border: 8px solid #111;
            border-radius: 40px;
        }

        header {
            background: linear-gradient(135deg, #e0e7ff 0%, #f5f0e8 50%, #fef2f2 100%);
            padding: 20px 24px;
            border-bottom: 2px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        [data-theme="dark"] header {
            background: linear-gradient(135deg, #1e1b4b 0%, #2d241e 50%, #450a0a 100%);
            border-bottom: 2px solid #475569;
        }

        .header-left { display: flex; align-items: center; gap: 15px; }
        header h2 { font-size: 1.25rem; color: var(--text-dark); }
        
        .back-btn, .close-btn { 
            cursor: pointer; 
            background: none; 
            border: none; 
            color: var(--text-dark); 
            transition: 0.2s;
        }
        
        .back-btn { font-size: 1.2rem; }
        
        .close-btn { 
            font-size: 1.5rem; 
            font-weight: bold; 
            opacity: 0.6; 
        }
        
        .close-btn:hover { 
            opacity: 1; 
            color: var(--danger); 
            transform: scale(1.1);
        }

        .content { flex: 1; overflow-y: auto; padding: 24px; }

        /* --- PROFILE UI --- */
        .profile-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .avatar-container {
            position: relative;
            width: 100px;
            height: 100px;
            margin: 0 auto 15px;
        }

        .avatar-img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid var(--bg-card);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .edit-avatar-btn {
            position: absolute;
            bottom: 0px;
            right: 0px;
            background: var(--primary);
            color: white;
            width: 28px;
            height: 28px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid var(--bg-card);
            cursor: pointer;
            font-size: 0.7rem;
        }

        .profile-grid {
            display: grid;
            gap: 16px;
            grid-template-columns: 1fr;
            margin-bottom: 24px;
        }

        @media (min-width: 600px) {
            .app-container:not(.mobile-mode) .profile-grid { 
                grid-template-columns: repeat(2, 1fr); 
            }
        }

        .info-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            padding: 14px;
            border-radius: 12px;
            transition: 0.2s;
        }

        .info-label {
            font-size: 0.65rem;
            font-weight: 700;
            color: var(--text-light);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 6px;
            display: block;
        }

        .info-input-group {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .info-input-group input {
            flex: 1;
            border: none;
            background: transparent;
            font-size: 0.95rem;
            color: var(--text-dark);
            font-weight: 500;
            outline: none;
        }

        .edit-icon { color: var(--primary); font-size: 0.8rem; cursor: pointer; opacity: 0.6; }

        /* --- INTEGRATED UNIQUE ID STYLES --- */
        .uid-card {
            background: var(--uid-gradient);
            border: 1px solid var(--border);
            padding: 20px;
            border-radius: 16px;
            margin-bottom: 24px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
        }

        .uid-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }

        .uid-label {
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 800;
            color: var(--text-light);
        }

        .uid-status {
            font-size: 0.65rem;
            background: #dcfce7;
            color: #166534;
            padding: 2px 8px;
            border-radius: 10px;
            font-weight: 700;
        }

        [data-theme="dark"] .uid-status {
            background: #064e3b;
            color: #34d399;
        }

        .uid-display {
            background: var(--bg-card);
            padding: 12px 16px;
            border-radius: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border: 1px solid var(--border);
        }

        .uid-display code {
            font-family: 'Monaco', monospace;
            font-size: 0.9rem;
            font-weight: 700;
            color: var(--text-dark);
        }

        .copy-btn {
            background: none;
            border: none;
            color: var(--primary);
            cursor: pointer;
            padding: 4px;
            display: flex;
            transition: transform 0.2s;
        }
        
        .copy-btn:active { transform: scale(0.85); }

        .uid-hint {
            font-size: 0.75rem;
            color: var(--text-light);
            margin-top: 10px;
            text-align: center;
        }

        /* --- SAVE SECTION --- */
        .save-section {
            margin-top: 10px;
            display: flex;
            justify-content: center;
        }

        .btn-save-profile {
            background: var(--primary);
            color: white;
            border: none;
            padding: 14px 50px;
            border-radius: 12px;
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 10px 15px -3px rgba(79, 70, 229, 0.3);
            transition: 0.3s;
            width: 100%;
        }

        @media (min-width: 600px) {
            .app-container:not(.mobile-mode) .btn-save-profile { width: auto; }
        }

        /* --- SWITCHES --- */
        .switch { position: relative; display: inline-block; width: 40px; height: 22px; }
        .switch input { opacity: 0; width: 0; height: 0; }
        .slider {
            position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0;
            background-color: #ccc; transition: .4s; border-radius: 24px;
        }
        .slider:before {
            position: absolute; content: ""; height: 16px; width: 16px; left: 3px; bottom: 3px;
            background-color: white; transition: .4s; border-radius: 50%;
        }
        input:checked + .slider { background-color: var(--primary); }
        input:checked + .slider:before { transform: translateX(18px); }

    </style>
</head>
<body data-theme="light">

    <div class="view-controls">
        <span>Web View</span>
        <label class="switch">
            <input type="checkbox" id="viewToggle" onchange="toggleView()">
            <span class="slider"></span>
        </label>
        <span>Mobile View</span>
    </div>

    <div class="app-container" id="appFrame">
        <header>
            <div class="header-left">
                <button class="back-btn" onclick="history.back()">←</button>
                <h2>My Profile</h2>
            </div>
            
            <div style="display: flex; align-items: center; gap: 20px;">
                <div style="display: flex; align-items: center; gap: 8px;">
                    <span style="font-size: 0.6rem; color: var(--text-light); font-weight: bold;">DARK</span>
                    <label class="switch">
                        <input type="checkbox" id="darkToggle" onchange="toggleDark()">
                        <span class="slider"></span>
                    </label>
                </div>
                <button class="close-btn" onclick="alert('Closing...')">✕</button>
            </div>
        </header>

        <div class="content">
            <div class="profile-header">
                <div class="avatar-container">
                    <img src="https://ui-avatars.com/api/?name=Alex+Johnson&background=4f46e5&color=fff" class="avatar-img" id="profilePic">
                    <label class="edit-avatar-btn">
                        ✎
                        <input type="file" hidden onchange="previewImage(event)">
                    </label>
                </div>
                <h3 style="color: var(--text-dark);" id="display-name">Alex Johnson</h3>
                <p style="color: var(--text-light); font-size: 0.8rem;">Member since Jan 2026</p>
            </div>

            <div class="profile-grid">
                <div class="info-card">
                    <span class="info-label">Full Name</span>
                    <div class="info-input-group">
                        <input type="text" value="Alex Johnson" id="inputName">
                        <span class="edit-icon">✎</span>
                    </div>
                </div>

                <div class="info-card">
                    <span class="info-label">Email Address</span>
                    <div class="info-input-group">
                        <input type="email" value="alex@example.com" id="inputEmail">
                        <span class="edit-icon">✎</span>
                    </div>
                </div>

                <div class="info-card">
                    <span class="info-label">Phone Number</span>
                    <div class="info-input-group">
                        <input type="text" value="+1 234 567 890" id="inputPhone">
                        <span class="edit-icon">✎</span>
                    </div>
                </div>

                <div class="info-card">
                    <span class="info-label">Username</span>
                    <div class="info-input-group">
                        <input type="text" value="@alex_gifts" id="inputUser">
                        <span class="edit-icon">✎</span>
                    </div>
                </div>
            </div>

            <div class="uid-card">
                <div class="uid-header">
                    <span class="uid-label">Member Identity</span>
                    <span class="uid-status">Verified</span>
                </div>
                <div class="uid-display">
                    <code id="uniqueId">UID-9982-PRO-001</code>
                    <button class="copy-btn" onclick="copyUID()" title="Copy ID">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                            <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                        </svg>
                    </button>
                </div>
                <p class="uid-hint">Share this ID to earn referral points.</p>
            </div>

            <div class="save-section">
                <button class="btn-save-profile" onclick="saveProfile()">Save Changes</button>
            </div>
        </div>
    </div>

    <script>
        function toggleView() {
            document.getElementById('appFrame').classList.toggle('mobile-mode');
        }

        function toggleDark() {
            const isDark = document.getElementById('darkToggle').checked;
            document.body.setAttribute('data-theme', isDark ? 'dark' : 'light');
        }

        function previewImage(event) {
            const reader = new FileReader();
            reader.onload = function() {
                document.getElementById('profilePic').src = reader.result;
            }
            reader.readAsDataURL(event.target.files[0]);
        }

        function copyUID() {
            const uidText = document.getElementById('uniqueId').innerText;
            navigator.clipboard.writeText(uidText).then(() => {
                const btn = document.querySelector('.copy-btn');
                const originalColor = btn.style.color;
                btn.style.color = "var(--success)";
                alert("ID Copied: " + uidText);
                setTimeout(() => { btn.style.color = originalColor; }, 2000);
            });
        }

        function saveProfile() {
            const newName = document.getElementById('inputName').value;
            document.getElementById('display-name').innerText = newName;
            
            const btn = document.querySelector('.btn-save-profile');
            const originalText = btn.innerText;
            btn.innerText = "Changes Saved! ✓";
            btn.style.background = "var(--success)";
            
            setTimeout(() => {
                btn.innerText = originalText;
                btn.style.background = "var(--primary)";
            }, 2000);
        }
    </script>
</body>
</html>
